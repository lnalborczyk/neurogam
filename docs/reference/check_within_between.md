# Test whether a predictor varies (or not) per participant

Test whether a predictor varies (or not) per participant

## Usage

``` r
check_within_between(
  data,
  participant,
  predictor,
  tol = 0,
  min_within_prop = 0.9
)
```
