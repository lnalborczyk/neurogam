% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.clusters_results_2d}
\alias{plot.clusters_results_2d}
\title{Plot time-generalisation GAM results (2D clusters)}
\usage{
\method{plot}{clusters_results_2d}(
  x,
  theme = ggplot2::theme_bw(),
  palette = "vik",
  midpoint = 0.5,
  axes_labels = c("Training time (s)", "Testing time (s)"),
  cluster_outline_colour = "black",
  ...
)
}
\arguments{
\item{x}{A \code{clusters_results_2d} object (typically returned by
\code{\link[neurogam]{testing_through_time}} for 2D time / time-generalisation).}

\item{theme}{A ggplot2 theme object added to each panel. Defaults to
\code{\link[ggplot2]{theme_bw}()}.}

\item{palette}{Character, a scico palette, see scico::scico_palette_show().}

\item{midpoint}{Numeric, the midpoint for diverging color palettes.}

\item{axes_labels}{Character vector, the x- and y-axis labels.}

\item{cluster_outline_colour}{Character. Colour used to outline cluster cells.
Defaults to \code{"white"}.}

\item{...}{Currently unused. Included for S3 compatibility.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object (a patchwork composition of two ggplots).
}
\description{
Visualises 2D (time-by-time) posterior predictions from a
\code{clusters_results_2d} object as a two-panel figure:
\enumerate{
  \item summary empirical data
  \item summary posterior predictions
}
}
\details{
Significant clusters stored in \code{x$clusters} are assumed to be
\strong{pointwise} (one row per time-by-time cell). Cluster locations are
overlaid as contour (which includes interpolation).

This method relies on \pkg{patchwork} to combine the two panels using \code{+}.
}
\examples{
\dontrun{
res2d <- testing_through_time(...)
plot(res2d)
}

}
