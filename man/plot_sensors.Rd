% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sensors.R
\name{plot_sensors}
\alias{plot_sensors}
\title{Plot EEG sensor positions on a 2D scalp}
\usage{
plot_sensors(
  sensors,
  x_col = "xproj",
  y_col = "yproj",
  label_col = "channel",
  show_head = TRUE,
  head_expand = 1.1,
  show_points = FALSE,
  point_size = 2,
  show_labels = TRUE,
  label_size = 3,
  label_repel = TRUE,
  label_only_highlight = FALSE,
  highlight = NULL,
  highlight_col = "orangered",
  highlight_size = 3,
  highlight_shape = 16,
  highlight_alpha = 1,
  dim_others = TRUE,
  other_alpha = 0.3,
  xlim = NULL,
  ylim = NULL
)
}
\arguments{
\item{sensors}{A data frame containing sensor coordinates. Must include the
columns specified by \code{x_col} and \code{y_col}. If \code{show_labels = TRUE}
and labels are desired, it must also include \code{label_col}.}

\item{x_col, y_col}{Character scalars giving the column names in \code{sensors}
corresponding to the x and y projected sensor coordinates.}

\item{label_col}{Character scalar giving the column name in \code{sensors} used
as sensor labels (e.g., \code{"channel"}).}

\item{show_head}{Logical; if \code{TRUE}, draw a head outline (circle + nose + ears).}

\item{head_expand}{Numeric scalar; multiplier applied to the maximum sensor radius
when computing the head radius. Values > 1 add padding around sensors.}

\item{show_points}{Logical; if \code{TRUE}, draw sensor markers for non-highlighted sensors.}

\item{point_size}{Numeric; size of non-highlighted sensor markers.}

\item{show_labels}{Logical; if \code{TRUE}, add sensor labels.}

\item{label_size}{Numeric; text size for sensor labels.}

\item{label_repel}{Logical; if \code{TRUE}, use \pkg{ggrepel} to reduce label overlap.
If \code{FALSE}, uses \code{\link[ggplot2]{geom_text}}.}

\item{label_only_highlight}{Logical; if \code{TRUE} and \code{highlight} is not \code{NULL},
show labels only for highlighted sensors.}

\item{highlight}{Optional vector specifying sensors to highlight. If numeric,
treated as row indices of \code{sensors}. Otherwise treated as sensor names
that are matched against \code{sensors[[label_col]]}.}

\item{highlight_col}{Character; colour used for highlighted sensors.}

\item{highlight_size}{Numeric; marker size for highlighted sensors.}

\item{highlight_shape}{Numeric; point shape for highlighted sensors (as in \code{ggplot2}).}

\item{highlight_alpha}{Numeric in [0, 1]; alpha transparency for highlighted sensors.}

\item{dim_others}{Logical; if \code{TRUE}, apply \code{other_alpha} to non-highlighted
sensor markers when \code{show_points = TRUE}.}

\item{other_alpha}{Numeric in [0, 1]; alpha transparency for non-highlighted sensors when
\code{dim_others = TRUE}.}

\item{xlim, ylim}{Optional numeric vectors of length 2 giving x and y limits.
If \code{NULL}, limits are computed automatically from the head radius (if
\code{show_head = TRUE}) or from the range of sensor coordinates otherwise.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualise the 2D layout of EEG sensors using projected coordinates (e.g.,
\code{xproj} and \code{yproj}). Optionally draws a head outline, sensor
markers, and/or sensor labels. A subset of sensors can be highlighted (by
name or by row index), with non-highlighted sensors optionally dimmed.
}
\details{
The head outline is computed from the maximum sensor radius (multiplied by
\code{head_expand}) using \code{st_head_radius()} and \code{st_head_outline()}.
These helper functions must be available in the calling environment.

Highlighting works in two modes:
\itemize{
  \item If \code{highlight} is numeric, indices are used to mark rows of \code{sensors}.
  \item If \code{highlight} is character, values are matched against \code{sensors[[label_col]]}.
}
}
\examples{
\dontrun{
# Basic sensor layout with head outline and labels
plot_sensors(sensors, show_points = TRUE, show_labels = TRUE)

# Highlight a few channels by name
plot_sensors(
  sensors,
  show_points = TRUE,
  show_labels = TRUE,
  highlight = c("CZ", "C1", "C2"),
  label_only_highlight = TRUE
  )

# Highlight by row index
plot_sensors(
  sensors,
  show_points = TRUE,
  highlight = c(10, 25, 42),
  highlight_col = "dodgerblue"
  )
}

}
\author{
Ladislas Nalborczyk \email{ladislas.nalborczyk@cnrs.fr}
}
